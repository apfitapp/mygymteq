{
  "name": "mygymtek",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel --filter \"@gym/*\" dev",
    "dev:web": "pnpm --filter @gym/web dev",
    "dev:api": "pnpm --filter @gym/api dev",
    "build": "pnpm --filter @gym/shared build && pnpm --filter @gym/api build && pnpm --filter @gym/web build",
    "build:web": "pnpm --filter @gym/shared build && pnpm --filter @gym/web build",
    "build:api": "pnpm --filter @gym/shared build && pnpm --filter @gym/api build",
    "typecheck": "pnpm --recursive run typecheck",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "db:migrate:local": "pnpm --filter @gym/api db:migrate:local",
    "db:seed:local": "pnpm --filter @gym/api db:seed:local"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.7",
    "@playwright/test": "^1.50.1"
  }
}
